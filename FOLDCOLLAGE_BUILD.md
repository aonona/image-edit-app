# FoldCollage build branch

This branch is used only for isolated FoldCollage APK builds. Main stays untouched.

Current source: v0.2 with movable divider lines, per-divider reset, and full position/zoom reset.
